{% extends "FunnyTripBundle::layout.html.twig" %}

{% block title %}
    {{ 'Mes Annonces'|trans }} | {{ parent() }}
{% endblock %}

{% block body %}

    <div class="page-header">
        <br>
        <h2>{{ 'Mes Annonces'|trans }}</h2>
        <hr/>
    </div>

    <div class="row">
        {% for annonce in annonces %}
            <div class="col-sm-6">
                <br>
                <div class="card">
                    <div class="card-block">
                        <div class="d-flex w-100 justify-content-between">
                            <h3 class="card-title">{{ annonce.villeDepart }} -> {{ annonce.villeArrivee }}</h3>
                            <small>{{ 'Publié le'|trans }} {{ annonce.date| date('d/m/Y') }}</small>
                        </div>

                        <p class="card-text">{{ 'Le'|trans }} {{ annonce.dateDepart| date('l  d F  Y') }}
                            {{ 'à'|trans }} {{ annonce.dateDepart| date('H:i') }}</p>
                        <p><span class="badge badge-default badge-pill">{{ annonce.prix }} €</span>
                            <small> | {{ annonce.nbPlaceMax - annonce.nbPlacePrise }} {{ 'place(s)'|trans }}</small>
                        </p>
                        <a href="{{ path('annonce_show', { 'id': annonce.id }) }}" class="btn btn-primary">{{ 'Voir'|trans }}</a>
                        <a href="{{ path('annonce_edit', { 'id': annonce.id }) }}" class="btn btn-warning">{{ 'Modifier'|trans }}</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}